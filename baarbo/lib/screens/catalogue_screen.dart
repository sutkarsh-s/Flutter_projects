import 'package:flutter/material.dart';

class CatalogueScreen extends StatelessWidget {
  static const routeName = '/salon-catalogue';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
